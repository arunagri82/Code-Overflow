def largest(arr,n): 
  max = arr[0] 
  for i in range(1, n): 
    if arr[i] > max: 
      max = arr[i] 
  return max
  
def main():
  arr = [1, 12, 123, 1234]
  n = len(arr) 
  Ans = largest(arr,n) 
  print ("Largest in given array is", Ans)

main()
